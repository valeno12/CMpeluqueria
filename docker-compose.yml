version: "3.9"
services:
  backend:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
  db:
    image: mysql:8
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: peluqueria
    ports:
      - "3306:3306"
